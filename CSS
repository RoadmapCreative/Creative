body, html {
    margin: 0;
    height: 100%;
}

.roadmap {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: navy;
    opacity: 0.5;
    z-index: -1;
    animation: animateRoadmap 10s infinite linear;
}

.content {
    text-align: center;
    padding: 50px;
    color: white;
}

@keyframes animateRoadmap {
    from {
        background-position: 0 0;
    }
    to {
        background-position: 100% 0;
    }
}
